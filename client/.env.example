# Production API endpoint (Vercel frontend -> Render backend)
# The backend routes already include the /api prefix, so do NOT add /api here.
VITE_API_URL=https://cookies-1-ex5p.onrender.com

# For local development, leave VITE_API_URL empty to use the Vite dev proxy
# which forwards /api/* to http://localhost:5000.
# VITE_API_URL=