{
  "name": "credential-session-manager-dashboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx concurrently \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
